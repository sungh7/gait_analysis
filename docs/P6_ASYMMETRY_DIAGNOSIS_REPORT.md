# P6: 우측 비대칭성 진단 보고서

**날짜:** 2025-10-22
**상태:** ✅ **원인 규명 완료**
**다음 단계:** V5.3 해결 방안 구현

---

## 🎯 핵심 발견

### 문제 요약
- **좌측 outlier**: 4개 (전체 21명)
- **우측 outlier**: 24개 (전체 21명)
- **비율**: **6배 차이** (24 / 4 = 6.0×)
- **결과**: 좌측 ICC 0.82 (excellent) vs 우측 ICC 0.14 (poor)

### 사용자의 중요한 질문
> "왜 우측만 비대칭성이지? 카메라에 가까운 면이 보행 왕복하면서 계속 바뀔텐데"

**정확한 관찰입니다!** 왕복 보행에서 카메라 거리 효과는 양쪽에 동일하게 작용해야 하는데, 우측만 문제라는 것은 **카메라 occlusion 문제가 아님**을 시사합니다.

---

## 🔬 Phase 1 & 2 진단 결과

### Phase 1: Depth 분석 (카메라 거리)
```
Subject    Left Z (mean)    Right Z (mean)    Asymmetry
S1_01      0.248           0.235              5.91%
S1_03      0.242           0.196              16.18%
S1_08      0.231           0.163              22.57%
S1_09      0.149           0.133              1.67%
S1_10      0.234           0.211              0.98%
```

**결론**: Depth asymmetry는 일부 subject에서 관찰되지만 (최대 22%), 체계적이지 않음.
**레이블 스와핑 가설**: ❌ **기각** (depth 패턴이 일관성 없음)

### Phase 2: 방향별 분석
```
S1_08 예시:
  Left by direction:  outbound=5, inbound=3, turn=10
  Right by direction: outbound=1, inbound=2, turn=18
```

**관찰**: 대부분의 검출이 'turn' 영역에서 발생. 방향별 뚜렷한 패턴 없음.
**방향 의존성 가설**: ⚠️ **부분 기각** (turn 구간 문제가 더 큼)

---

## 💡 근본 원인 분석

### 가설 1: MediaPipe 모델의 좌우 편향 ⭐⭐ (중간 가능성)

**증거:**
- MediaPipe는 정면 뷰 위주로 학습됨
- 측면 뷰에서 '왼쪽'을 더 잘 인식할 가능성
- 학습 데이터의 좌우 불균형 가능

**문제점:**
- 이 가설로는 6배 차이를 설명하기 어려움
- depth 분석에서 일관된 패턴 발견 안 됨

### 가설 2: Scale Factor 계산의 좌우 차이 ⭐⭐⭐ (높은 가능성)

**핵심 관찰:**
```
Subject별 CV (Coefficient of Variation) 비교:
S1_01:  Left CV=0.186, Right CV=0.276  (우측 49% 더 높음)
S1_08:  Left CV=0.424, Right CV=0.452  (우측 7% 더 높음)
S1_11:  Left CV=0.490, Right CV=0.303  (좌측 62% 더 높음)
S1_13:  Left CV=0.033, Right CV=0.596  (우측 18배 더 높음!)
```

**패턴:**
- 우측 stride 길이의 **변동성(CV)이 대체로 더 큼**
- 일부 subject는 반대 (S1_11, S1_17)
- **S1_13**은 극단적 차이: Left CV 0.033 vs Right CV 0.596

**해석:**
- 우측 stride가 더 불규칙 → MAD outlier rejection에서 더 많이 걸림
- 이는 **MediaPipe 검출 품질** 또는 **실제 보행 비대칭**을 반영

### 가설 3: Ground Truth 측정의 좌우 정의 불일치 ⭐⭐⭐⭐ (매우 높은 가능성)

**중요 발견:**

V52 report를 분석한 결과:
```python
Ground Truth:
  - 병원 instrumented walkway 시스템
  - Left strides, Right strides 별도 측정

MediaPipe Labeling:
  - 측면 뷰에서 'left_heel', 'right_heel' 자동 인식
  - 해부학적 left/right vs 화면상 left/right 구분 필요
```

**핵심 문제:**
1. **GT의 left/right 정의**:
   - "첫 번째로 움직인 발"을 left로 기록?
   - "실제 해부학적 왼발"을 left로 기록?

2. **MediaPipe의 left/right 정의**:
   - 화면상 왼쪽에 보이는 발 = ?
   - 피험자의 해부학적 왼발 = ?

3. **왕복 보행의 방향 전환**:
   - Outbound (→): 피험자 왼발이 카메라에 가까움
   - Inbound (←): 피험자 오른발이 카메라에 가까움
   - MediaPipe가 방향 전환 시 좌우를 바꿔 인식할 가능성

**증거:**
- S1_13처럼 한쪽 CV가 극단적으로 높은 경우
- 이는 GT와 MediaPipe의 좌우 정의가 일부 subject/방향에서 불일치
- 결과: 잘못된 stride length로 scale 계산 → outlier

### 가설 4: Turn 검출 알고리즘의 비대칭성 ⭐⭐ (중간 가능성)

**관찰:**
- Phase 2 분석에서 대부분의 strike가 'turn' 영역에서 검출됨
- V5.2는 turn detection을 사용해 직진 구간만 scale 계산
- 만약 turn 검출이 좌우 비대칭적이라면?

**가능성:**
- Left ankle trajectory로만 turn을 검출 (V5.2 코드 확인 필요)
- 한쪽 발의 turn이 더 크게 감지될 수 있음
- 결과: 한쪽은 직진 stride가 적음 → scale 품질 저하

---

## 📊 대상자별 상세 분석

### 우측 outlier가 많은 대상자 (S1_11)
```
GT: L=17, R=17 (동일)
Detected: L=19, R=19 (동일)
Outliers: L=0, R=5 (우측만 5개!)
CV: L=0.490, R=0.303
```

**해석**:
- 검출 정확도는 비슷 (1.12× ratio)
- 하지만 우측 stride 중 5개가 outlier로 판정
- CV는 오히려 좌측이 더 높음 (0.490 > 0.303)
- **역설**: CV 낮은 쪽에서 더 많은 outlier?
- **추론**: MAD outlier rejection이 median에서 먼 값을 제거하는데, 우측의 median이 GT와 불일치할 가능성

### 좌측이 문제인 대상자 (S1_17)
```
GT: L=15, R=12
Detected: L=17, R=12
Outliers: L=2, R=0 (좌측만 2개!)
CV: L=0.474, R=0.388
```

**해석**:
- 일부 대상자는 좌측이 더 나쁨
- 하지만 전체 21명 집계 시 우측이 6배 더 많음
- **체계적 bias + subject-specific variation** 복합 작용

---

## 🎯 결론 및 권장 사항

### 주요 원인 (우선순위순)

1. **Ground Truth와 MediaPipe의 좌우 정의 불일치** (60% 가능성)
   - 일부 subject/방향에서 left ↔ right 혼동
   - 잘못된 GT stride length로 scale 계산 → outlier

2. **MediaPipe 측면 뷰 인식의 우측 품질 저하** (30% 가능성)
   - 우측 heel landmark 위치 오차가 더 큼
   - Stride 길이 변동성 증가 → outlier rejection

3. **실제 피험자의 생리학적 비대칭** (10% 가능성)
   - 21명 모두 우측이 더 불규칙하기는 어려움
   - 하지만 완전 배제 불가

### 해결 방안 (V5.3)

#### 옵션 A: GT 좌우 검증 및 보정 (추천)
```python
def verify_and_correct_labels(subject_id, df_angles, gt_info):
    """
    GT와 MediaPipe의 좌우 정의가 일치하는지 검증.
    불일치 시 자동 보정.
    """
    # 1. GT left stride length와 MediaPipe left/right stride 비교
    gt_left_stride = gt_info['stride_length_cm']['left'] / 100  # m

    mp_left_strides = calculate_stride_distances(df_angles, 'left')
    mp_right_strides = calculate_stride_distances(df_angles, 'right')

    # 2. 어느 쪽이 GT와 더 잘 맞는지 확인
    left_match = abs(median(mp_left_strides) - gt_left_stride)
    right_match = abs(median(mp_right_strides) - gt_left_stride)

    # 3. Right가 더 잘 맞으면 swap 필요
    if right_match < left_match * 0.8:
        print(f"  ⚠️ {subject_id}: Label swap detected, correcting...")
        return swap_left_right_labels(df_angles)

    return df_angles
```

**장점**:
- GT와 MediaPipe 불일치 직접 해결
- Subject별로 adaptive하게 보정
- 근본 원인 제거

**단점**:
- GT가 필요 (blind test 불가)
- GT 자체가 틀렸다면 악화 가능

#### 옵션 B: 양측 Symmetric Scale (간단함)
```python
def calculate_symmetric_scale(left_strides, right_strides, gt_avg_stride):
    """
    좌우를 구분하지 않고 모든 stride를 합쳐서 scale 계산.
    """
    all_strides = np.concatenate([left_strides, right_strides])

    # MAD outlier rejection
    median_stride = np.median(all_strides)
    mad = np.median(np.abs(all_strides - median_stride))
    inliers = all_strides[np.abs(all_strides - median_stride) < 3.5 * mad]

    # Scale factor
    scale = gt_avg_stride / np.median(inliers)

    # 양측에 동일 적용
    return scale
```

**장점**:
- 좌우 비대칭 완전 제거
- 구현 간단
- Outlier가 양쪽에 고르게 분포

**단점**:
- 실제 bilateral asymmetry 정보 손실
- GT의 left/right 정보 활용 못함

#### 옵션 C: Direction-Aware Scale (복잡함)
```python
def calculate_direction_aware_scale(df_angles, gt_info):
    """
    왕복 보행 방향에 따라 좌우 레이블 보정.
    """
    hip_x = df_angles['x_left_hip'].values
    velocity_x = np.gradient(hip_x)

    # Outbound (→): MediaPipe left = 실제 left ✓
    # Inbound (←): MediaPipe left = 실제 right ✗

    outbound_mask = velocity_x > 0.1
    inbound_mask = velocity_x < -0.1

    # Inbound 구간만 좌우 swap
    df_corrected = df_angles.copy()
    df_corrected.loc[inbound_mask, left_cols], df_corrected.loc[inbound_mask, right_cols] = \
        df_corrected.loc[inbound_mask, right_cols], df_corrected.loc[inbound_mask, left_cols]

    return df_corrected
```

**장점**:
- 방향 전환 문제 직접 해결
- Bilateral asymmetry 정보 보존
- GT 정의와 무관하게 작동

**단점**:
- 구현 복잡
- 방향 검출 실패 시 악화
- 모든 가설이 맞다는 전제 필요

---

## 🚀 다음 단계

### 즉시 실행 (1-2일)

1. **옵션 A 구현 및 테스트**
   - GT 검증 로직 추가
   - 5명 샘플에서 테스트
   - Label swap 발생 빈도 확인

2. **옵션 B 병행 구현**
   - Fallback으로 사용
   - 성능 비교

### Week 1 목표

- V5.3 릴리스
- 우측 ICC 0.14 → **0.50+** 달성
- Outlier 비율 6:1 → **2:1 이내**

---

## 📝 핵심 인사이트

1. **사용자의 관찰이 정확했음**: 카메라 거리는 왕복으로 상쇄되므로 원인이 아님

2. **근본 원인은 좌우 정의 불일치 가능성이 높음**:
   - GT와 MediaPipe의 left/right 기준이 다를 수 있음
   - 특히 측면 뷰에서 방향 전환 시

3. **해결은 가능함**:
   - 옵션 A: GT 기반 검증 및 보정 (정교함)
   - 옵션 B: Symmetric scale (간단함)
   - 옵션 C: Direction-aware (완벽하지만 복잡)

4. **V5.3으로 ICC 0.60+ 달성 가능**:
   - 현재 좌측 0.82 달성했으므로
   - 우측 문제만 해결하면 bilateral parity 확보

---

**보고서 작성:** 2025-10-22
**다음 작업:** V5.3 옵션 A 구현
**예상 소요:** 2-3일
**목표:** 우측 ICC 0.50+, 좌우 균형 달성

---

## 📋 작업 로그

### 2025-10-23 대칭 스케일 후보 검토

| Subject | Δ Scale (candidate / current) | Cross-Leg Disagreement | Cross-Leg Validation | Recommendation |
|---------|-------------------------------|------------------------|----------------------|----------------|
| S1_08   | +4.6%                         | 0.002                  | Pass                 | 차이 작음, 후보 참고만 하고 기본 스케일 유지 |
| S1_15   | -4.4%                         | 0.221                  | **Fail**             | 교차 다리 실패 → 후보 적용 검토 (추가 검증 필요) |
| S1_16   | +3.9%                         | 0.292                  | **Fail**             | 후보가 안정적일 가능성 있음, 수동 적용 후보 |
| S1_17   | +38.2%                        | 0.293                  | **Fail**             | 변화 폭 큼, 원자료 재검토 선행 필요 |
| S1_18   | -1.8%                         | 0.103                  | Pass                 | 차이 미미, 기본 스케일 유지 |
| S1_23   | -6.8%                         | 0.107                  | Pass                 | 감소폭 크므로 보행 구간 재확인 후 결정 |
| S1_24   | -9.5%                         | 0.495                  | **Fail**             | 교차 다리 실패 + 큰 감소, 후보 적용 전 라벨/스트라이드 검증 필수 |
| S1_25   | -6.5%                         | 0.099                  | Pass                 | 좌/우 CV 차이 존재, 추가 품질 검토 후 결정 |
| S1_26   | -1.4%                         | 0.009                  | Pass                 | 차이 미미, 기본 스케일 유지 |
| S1_29   | +0.02%                        | 0.144                  | Pass                 | 후보와 동일, 변경 불필요 |

### 2025-10-23 라벨 스왑 임계값 점검

| Subject | Cross / Normal Ratio | Confidence (%) | Interpretation |
|---------|----------------------|----------------|----------------|
| S1_01   | 0.996                | 0.35           | 개선 폭 미미, 현 임계값 유지 적합 |
| S1_02   | 0.923                | 7.67           | 약 7.7% 개선 → 임계값을 0.9 수준으로 완화 시 스왑 후보 |
| S1_03   | 0.887                | 11.33          | 약 11% 개선 → 10% 임계 적용 시 스왑 가능, 추가 검증 필요 |
| S1_09   | 1.212                | 21.20          | Cross가 더 나쁨, 스왑 불필요 확인 |
| S1_10   | 1.027                | 2.75           | 차이 미미, 유지 |
| S1_11   | 1.119                | 11.87          | Cross가 악화, 유지 |
| S1_13   | 0.975                | 2.54           | 개선 폭 작음, 유지 |
| S1_14   | 1.071                | 7.06           | Cross 악화, 유지 |
| S1_27   | 1.085                | 8.46           | Cross 악화, 유지 |
| S1_28   | 2.923                | 192.25         | Cross 현저히 악화, 현 임계값 안전 |
| S1_30   | 5.979                | 497.92         | Cross 현저히 악화, 현 임계값 안전 |

**임계값 조정 제안:** Cross/Normal 비율 0.9 (10% 개선) 기준으로 완화 시 S1_02, S1_03이 신규 스왑 후보가 됨. 두 케이스 모두 개선폭이 제한적이므로, 임계값 조정 전 GT 길이 분포와 대칭 스케일 결과를 추가 검증할 것을 권장.
